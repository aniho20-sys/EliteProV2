{
  "name": "elitepro-functions",
  "description": "Cloud Functions for ElitePro push notifications",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "jest --testPathPattern=test/ --forceExit",
    "test:emulator": "firebase emulators:exec --only firestore --project elitepro-16718 'npm test'"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^6.3.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.5.0",
    "jest": "^29.7.0"
  }
}
