# 全 App 完善清單 + 建議 Skills（2026-09-26）

Ani 要求：「將成個 app 你覺得需要完善嘅地方講出嚟，同埋喺 GitHub 搵適合呢個 project 用嘅 skills」。
每項都附證據（邊個檔案／邊行／跑咗咩指令）。未證實嘅標明「未證實」。

---

## 🔴 上 live 收真錢之前一定要做

| # | 問題 | 證據 | 後果 |
|---|---|---|---|
| 1 | **學生刪帳戶，GoCardless 月費唔會取消** | `functions/index.js` `onAccountDelete`（約第 72 行）只刪 messages / workoutLogs / schedule / workoutPlans / exercises，冇掂 `subscriptions`，亦冇 call GoCardless 取消 | 人走咗，銀行照每月扣錢 |
| 2 | **刪帳戶留低健康資料同財務記錄** | 同一個 function 冇刪 `intakeForms`（傷患、PAR-Q）、`invoices`、`creditLedger`、`templates`、`exerciseOverrides`。只有 admin 清假帳號嗰條（約第 1150 行）先會刪 | 英國 GDPR：健康資料係特殊類別，用戶要求刪除就要刪 |
| 3 | **Phase 3 Step 4 / 5 未做** | 付款通知（webhook）→ 自動加堂；暫停／取消 | 冇 Step 5，學生喺 app 入面取消唔到月費 |
| 4 | **CI 冇跑任何測試就直接 deploy 上 production** | `.github/workflows/firebase-hosting.yml` 只有 build + deploy，冇 `vitest`、冇 functions 測試、冇 rules 測試。Functions 仲用 `--force` | 458 條測試（前端 282 + functions 107 + rules 69）全部只喺 agent session 手動跑，漏跑一次就會 ship 壞嘢 |

## 🟠 工程／流程

| # | 問題 | 證據 |
|---|---|---|
| 5 | **「每個 PR 自動 review（員工C + 員工F）」唔存在** | `.github/` 只有一個 workflow，冇 review 相關；而且所有改動直接 push 主線，根本冇 PR。CLAUDE.md「運作模式」段寫咗但冇實現 |
| 6 | **Lint「226 個 pre-existing error」其實係 config 問題** | `npx eslint src functions`：301 個 error，297 個係 `functions/` 冇設 Node globals（`no-undef` 對 `require`/`module`）。`src/` 本身只有 3 個，其中一個係重覆翻譯 key `plans.watch_demo`（`en.js` 第 100 + 130 行，`zh-HK.js` 第 185 + 213 行，兩個值一樣所以暫時冇影響）。修好 config 之後 lint 先可以放入 CI |
| 7 | **冇錯誤監察** | `src/` 冇任何 crash reporting。09-18 白畫面係你部電話見到先知 |
| 8 | **Firestore 有冇備份：未證實** | Repo 冇任何備份設定；GCP console 有冇開 PITR / 定時 export，呢個 session 睇唔到 |
| 9 | **Root 有雜物** | `g.sh`（會將 project Editor 權限授予 service account 嘅 script）、`05339FDC-….png` 被 commit 咗入 repo |
| 10 | **兩個頁面太大** | `ClientDetailPage.jsx` 1045 行、`ProfilePage.jsx` 1015 行 —— 越大越易改一處壞另一處 |
| 11 | **CLAUDE.md 有過時描述** | 第 587 行寫「users: Any auth can read」，但 `firestore.rules` 早已收窄（只讀自己、自己學生、自己教練） |

## 🟡 產品（部分已喺 BACKLOG）

- **B10** 月度報告 iPhone 撳咗冇反應（`win.print()`，常規 #30）—— 等你答 B11 語言問題
- **徽章**：每次記錄訓練都寫入 `users.badges`，但冇任何畫面顯示
- **月費設定入口難搵**（09-23 實際撞到）：「標記為測試者」掣要先儲存收費先會出現，但個畫面冇提示。建議：未設收費時顯示一句「先去 Profile 設定每月計劃收費」

---

## 建議 Skills（GitHub）

⚠️ Skill 係畀 agent 跟嘅指示，同 agent 有一樣權限。只揀官方或者有信譽嘅來源，安裝前逐個讀過。
安裝係 agent 做（放入 `.claude/skills/` commit 上 repo），唔使你開 terminal（常規 #26）。

| 優先 | Skill | 來源 | 點解啱呢個 project |
|---|---|---|---|
| ⭐ 1 | `firebase-security-rules-auditor` | [firebase/agent-skills](https://github.com/firebase/agent-skills/tree/main/skills/firebase-security-rules-auditor)（Firebase 官方） | 專門 red-team `firestore.rules`：create/update 唔一致、自己俾自己權限、欄位驗證。我哋 rules 有 12 個 collection，而且收錢之後風險更高 |
| ⭐ 2 | `firestore-rules-creation` | [firebase/agent-skills](https://github.com/firebase/agent-skills/tree/main/skills/firestore-rules-creation) | Step 4/5 會加新 collection，寫 rules 時用 |
| ⭐ 3 | `webapp-testing` | [anthropics/skills](https://github.com/anthropics/skills/tree/main/skills/webapp-testing)（Anthropic 官方） | 用 Playwright 真係喺瀏覽器撳掣。捉唔到 iPhone 專有問題（常規 #36 仍然要你真機），但可以捉「撳咗冇反應」「白畫面」呢類 |
| ⭐ 4 | `verification-before-completion`、`systematic-debugging` | [obra/superpowers](https://github.com/obra/superpowers/tree/main/skills) | 同常規 #36 / #40 / #45 同一個精神：未證明就唔准講「搞掂」。09-23 我先估錯一次原因先搵到真 bug，呢個 skill 就係防呢種 |
| 5 | `firebase-firestore` | [firebase/agent-skills](https://github.com/firebase/agent-skills/tree/main/skills/firebase-firestore) | Offline persistence、listener、permission-denied 除錯 —— 09-23 嗰個 listener race 就係呢類 |
| 6 | `supply-chain-risk-auditor`、`property-based-testing` | [trailofbits/skills](https://github.com/trailofbits/skills)（Trail of Bits，安全公司） | 審 npm 依賴；property-based testing 啱測堂數／月費計算呢類數學邏輯 |
| — | 目錄（揀更多用） | [VoltAgent/awesome-agent-skills](https://github.com/VoltAgent/awesome-agent-skills) | 1000+ 個，質素參差，只做參考 |

已經有、唔使再裝：`impeccable`（設計）、`.claude/agents/ext-*` 五個專家角色（付款、i18n、權限、E2E、安全掃描）。

唔建議：`firebase-crashlytics` —— Crashlytics 唔支援 web app。
