# [員工A - SA] 本週系統週報

**日期**：2026-08-03（提早開會，原定 8-04 星期一）
**涵蓋期**：2026-07-27 → 2026-08-03

---

## 【1. 系統狀態】

### 本週改動／deploy

| 日期 | 改動 | 類型 |
|---|---|---|
| 07-28 | Code health audit（`reports/code-health-audit-2026-07-28.md`）+ Top 5 修復 | 技術債 |
| 07-28 | `utils/currencyUtils.js`：`formatCurrency()` 統一全站金額顯示 | Bug fix（真 bug） |
| 07-29 | 🔴 `firestore.rules` field allowlist 漏 `intakeCompleted` —— 新學生 100% 卡死 | 嚴重 bug fix |
| 07-29 | `deploy_functions` IAM 修復（Cloud Functions Admin 落 `firebase-adminsdk-fbsvc@`） | CI |
| 07-29 | Invoice PDF：`pdf-lib` 客戶端生成 + `navigator.share` | 新功能 |
| 07-31 | Onboarding 問卷版面重做 + `TrainingProfilePage` + `/training-profile` | 新功能 |
| 08-01 | 續約提示改為持續顯示（≤5 堂每次彈 + book 時提示） | 產品改動 |
| 08-01 | Credit 透支 booking（1 堂上限）+ CLAUDE.md #32/#33 | 新功能 + 規則 |
| 08-02 | 透支 booking 兩個真機 bug 修復 + server-side 強制 | Bug fix |
| 08-02 | `.modal-overlay` 手機置中（影響全 app modal） | UI fix |

### Rules / Functions 更新

- `firestore.rules`：`users` self-update allowlist 加 `intakeCompleted` / `businessName` / `currency`
- `functions/index.js`：`onScheduleBooked` 加 server-side 透支上限 + `creditLedger` 欠款記錄；`onScheduleCreditUpdate` 加 reopen 退款分支

### 測試覆蓋

| 套件 | 上週 | 本週 |
|---|---|---|
| Cloud Functions（emulator） | 22 | **35** |
| Firestore rules（emulator） | 33 | **37** |

### 現有 Bug／技術債狀態

- ✅ 已清：`badgeUtils` UTC 日期 bug、`PaymentSheetModal` 寫死 `£`、`/apply` 漏 `GYMLA_ENABLED` gate、`TrainerDashboard` 硬編 threshold
- 🟡 未清：Session 剩餘數計算仍有多處實現（audit 已列，未統一）；badges 有寫入邏輯但零 UI（Ani 已批「留返，最小 UI 入 backlog」）
- 🟡 未清：`GYMLA_ENABLED` 底下 operator 三態問題 —— Ani 決定 Phase 5 開之前先處理

---

## 【2. 各崗位進度】

- **員工B（Dev）**：本週最重手 —— invoice PDF、onboarding 重做、透支 booking、currency 統一
- **員工C（Reviewer）+ 員工A（SA）**：完成 code health audit，Top 5 全部由 Ani 批准並執行
- **員工D（UI/UX）**：問卷版面重做（44px 觸控 pill、24px section 間距、兩步分頁），出 mockup screenshot 先落實
- **員工E（QA）**：guardian test 機制 —— 注入 bug 驗證 test 真係 fail 得（一個未 fail 過嘅 test 等於冇 test）
- **員工F（Security）**：本週無獨立 review；但透支 server-side 強制屬安全邊界改動，威脅模型已重寫
- **員工X（Marketing）**：7-31 週報已出；7-24 週報遺留喺孤兒分支（見下）

---

## 【3. 待 Ani 拍板】

1. **孤兒分支處理**（已列完整清單交 Ani，見下方附錄）
2. **Phase 3 Step 3**（訂閱管理 UI + mandate 創建）幾時開？現時 Step 1-2 已 live 但 **Connect 掣未有真人試過 sandbox flow** —— 建議先實測再開 Step 3，否則 Step 3 建喺未驗證嘅地基上
3. **Backlog #19 badges 最小 UI** —— 學生數據一直喺度累積但完全冇得睇，愈拖愈似死功能
4. Phase 1 剩餘真機場景（Landing Page → signup → 全程）幾時做

---

## 【4. 下週建議優先次序（系統分析角度）】

1. **真機驗證本週三批改動** —— 透支修復、modal 置中、Training Profile 入口。本週兩個 bug 都係真機先揪到，自動化 test 覆蓋唔到 UI 層時序
2. **GoCardless Connect sandbox flow 實測** —— Phase 3 唯一未驗證嘅環節，係 Step 3 嘅前置
3. **Session 剩餘數計算統一** —— audit 已列位置，係 Phase 5 之前必清嘅技術債（外部教練入場後行為唔一致就係產品 bug）
4. **孤兒分支清理** —— 等 Ani 批完即刻做

---

## 附錄：孤兒分支清單（相對 `claude/fitness-app-features-LbxtG`）

| 分支 | 未 merge commit | 內容 | 建議 |
|---|---|---|---|
| `archive/parallel-credit-system-2026-07` | 10 | 2026-07-07 平行 credit system 實驗，最後 commit「refactor: move creditBalance migration from client-side to…」 | 保留做 archive，唔 merge |
| `claude/affectionate-cerf-yaz363` | 1 | `6846001` 員工X 7-24 Marketing 週報（`reports/marketing-report-2026-07-24.md`，+21 行） | **cherry-pick 過 production 後刪** |
| `claude/magical-wright-b8m3i8` | 1 | `ec3fc49` 員工A 8-03 週報（12 行精簡版，已由本報告取代） | 刪 |
| `claude/affectionate-cerf-utjhgq` | 0 | 已完全 merge（7-17 Marketing 週報） | 刪 |
| `claude/read-claude-progress-md-8xs16x` | 0 | 已完全 merge | 刪 |
| `gh-pages` | 1 | 2026-03-30 GitHub Pages 部署產物，非源碼分支 | 保留唔動 |

**根因**：三個 Routine（SA / CEO / Marketing）冇指定 branch，每次自動開工都開新分支。已向 Ani 提供要加入 Routine prompt 嘅句子。
