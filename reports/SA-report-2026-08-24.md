# [員工A - SA] 📋 本週系統週報（2026-08-24）

> ⚠️ **補開。** 呢個會原定 2026-08-24（星期一）10:00，當日冇開，冇出報告。呢份係 08-27（星期四）補寫，覆蓋 08-18 → 08-27 呢十日。同一個星期嘅 Marketing 週報（星期五 08-21）一樣冇開，最後一份仲係 `marketing-report-2026-08-14.md`。
>
> 覆蓋期：`2fef159`（08-18）→ `91c550d`（08-25），共 20 個 commit。

---

## 【1. 系統狀態】

### 1.1 本週改動（20 個 commit，全部已 merge 落 `claude/fitness-app-features-LbxtG`）

| 日期 | 主題 | 內容 |
|---|---|---|
| 08-18 | GoCardless | 查證報告（partner 批核 / sandbox 收費 / fallback 三條問題）+ 重寫過時嘅 sandbox setup guide |
| 08-18~20 | Landing page | 由真 component render 截圖、修正第 2/3 張圖錯用學生端畫面、修 plan name 喺深色卡上近乎黑字 |
| 08-19 | CLAUDE.md #37 | 七條自審標準寫入常規 |
| 08-20 | 自助註冊 | Landing 轉全自助、加 `onNewTrainerSignup`（教練註冊即通知 owner） |
| 08-22 | 教練端清理 | 刪 Profile founding banner、debug 掣收入 dev flag、Backup 文案重寫、Auto-Classify 冇嘢分類就自動收埋 |
| 08-22 | Founding 計數修正 | 由「數教練文件」改為「數註冊事件」（`platformEvents`）—— 呢個係我上一輪自己寫落去嘅 bug |
| 08-22 | Owner 工具 | account audit + Auth 訊號 + 兩步式測試帳號清理，並寫入 CLAUDE.md #38 |
| 08-24~25 | Password reset | 停止喺 Firebase 唔知情況下聲稱「已寄出」、加 lookup UI、出報告、**更正報告**（真因係寄件人名唔係 ElitePro） |

### 1.2 驗證狀態（逐項講點驗，唔係報「過」）

| 項目 | 結果 | 點驗 |
|---|---|---|
| 前端測試 | ✅ **110/110 通過** | `npm test`（vitest run），今日 08-27 08:11 跑過，5 個檔案 |
| ESLint（`src/`） | ✅ **0 error** | `npx eslint src` —— 得 1 個 warning，係 `useRestTimer.js:155` 一句已經冇用嘅 `eslint-disable` |
| ESLint（全 repo） | 🟡 254 error，全部喺 emulator 測試目錄 | `npx eslint . -f json` 逐檔分類：`functions/test` 149、`firestore-tests` 100、`design-ref` 4、`functions/jest.config.js` 1。**全部係 Node/Jest global 未設定**，唔係真 bug。上週報 226，升到 254 係因為新增咗 rules 測試檔，唔係新 error 混入 app code |
| Cloud Functions 測試 | ⚠️ **今日跑唔到** | `npm run test:emulator` → `sh: 1: firebase: not found`。呢個 container 冇裝 Firebase CLI。最後一次確認全綠係本 session 較早前（35/35） |
| Firestore rules 測試 | ⚠️ **今日跑唔到** | 同上，同一個原因 |
| CI | ✅ **連續 30 次全綠** | GitHub Actions run #481–#510 全部 `success`，最後一次 deploy 08-25 17:12 |

**要講清楚：** 上面兩個 ⚠️ 我唔會當「通過」。今日呢個 container 冇 Firebase CLI，我跑唔到就係跑唔到，唔可以憑上次結果報今次。

### 1.3 一個查完發現係我錯嘅推論（記低，因為過程有用）

我本來想報一個風險：`cd functions && npm test`（唔加 `:emulator`）會唔會誤中副車撞落 production？跑落去見到 `MetadataLookupWarning: Request failed with status code 502` —— 睇落好似 Admin SDK 喺度搵真嘅 GCP credential，即係 #38 嗰種情境。

**查完係我諗多咗。** `functions/test/bookSession.test.js:34` 同 `gcOAuthNonce.test.js:17` 兩個檔案都喺 `require('firebase-admin')` **之前**先寫死 `process.env.FIRESTORE_EMULATOR_HOST = 'localhost:8080'`，所以 Firestore 嘅讀寫一定去 emulator，冇 emulator 就 hang 住，永遠去唔到 production。

所以呢條記做「浪費時間」，唔係「安全風險」——降級處理，唔好當成緊急。差別在於前者可以排落 backlog，後者要即刻停低。

---

## 【2. 各崗位進度】

- **員工B（Dev）** —— 本週主力。修咗 landing 截圖／對比度／founding 計數／password reset 提示，起咗 owner 工具（stats、audit、lookup、清理）。
- **員工E（QA）** —— 同 B 一齊查 password reset。查完最大收穫唔係 code：**「用戶收唔到」同「用戶搵唔到」由 code 嗰邊睇落去一模一樣**，兩個 case 都係 resolve 成功、冇 error、學生話冇收到。唯一分辨方法係問返用戶本人。呢條記入 `password-reset-2026-08-23.md` §9.4。
- **員工C（Reviewer）** —— 本週三個報告都自己審完先交（#37）。
- **員工D（UI/UX）** —— 教練端清理（08-22）、landing 對比度修正。
- **員工F（Security）** —— 冇工作。有一項擱住（見第 3 節第 2 條）。
- **員工X（Marketing）** —— **08-21 週報冇出**，斷咗。上一份 08-14 嘅「下週行動」冇人交數。

---

## 【3. 待 Ani 拍板】

排咗優先次序 —— 頭兩條會擋住其他嘢，後面嗰啲唔急。

**1. Firebase Console → Authentication → Templates → Password reset → sender name 改做 `ElitePro`**
唔係拍板，係得你做到嘅動作（Console 設定，唔喺 repo）。App 嗰邊已經加咗提示叫人搵「password reset」同話寄件人可能唔係 ElitePro，但嗰句只係後備。真正根治係改個名。改完用 `hkerwillfight@gmail.com` 試一次就係 #36 驗收。

**2. `users` read rule 收唔收緊**
而家 `allow read: if isAuth()`，即係任何登入用戶讀到全部人嘅 email。但 #34 嗰個 invite code 查詢**就係靠呢條規則**先運作到（未連結嘅學生同教練冇任何關係，只能靠 query）。收緊 = 有機會再整死註冊流程，所以要連 rules 測試一齊做，唔可以直接改。**要你話收唔收，我唔會自己決定。**

**3. 測試帳號清理未執行**
個掣 08-22 已經上線（Profile → Platform Stats → "Show what would be deleted" → 確認）。你未撳過。撳完話我知刪咗幾多個，我先更新得 PROGRESS.md。

**4. GoCardless 卡收唔收埋 flag** —— 未接完，新教練見到會撳。
**5. robots.txt** —— landing 已上線，未決定畀唔畀 index。
**6. 兩項小嘢** —— Profile 空白嘅 `Age: | Height: cm | Goals:` 一行；一個 nested `<button>` warning。

---

## 【4. 下週建議優先次序】

**1. 更新 PROGRESS.md** —— 停喺 08-13，落後 **26 個 commit**。CLAUDE.md 自己寫住「所有 agent 開工前必讀」，同時又寫住過時嘅 PROGRESS.md 試過令 audit 判斷出錯。而家佢就係嗰個狀態。呢條係第一位，因為佢會令之後每一個 session 判斷錯。

**2. GoCardless sandbox 真人實測** —— 已經拖咗**兩個星期**（08-17 報告已列第一位）。Phase 3 Step 3 開唔到工。連續兩份週報同一項目排第一而冇郁過，本身就係一個要處理嘅訊號。

**3. 補真機驗收清單** —— 積落嚟未撳過嘅：filter chips、續約 modal、Analytics retention/revenue、Client Detail body stats、教練 "Active this week"、Workout Plans 對比度、清理後嘅 Profile、Platform Stats、新 reset 文案。呢啲全部 #36 話明只有真機撳先算數，我做唔到。

**4. 清 3 條分支** —— `qlhyuu`、`pjh169`（兩條都係上上週建議刪，未刪）、`read-claude-progress-md-8xs16x`。

**5. Marketing 週報接返上** —— 08-21 斷咗一次。#格式要求第 1 節要逐項交返 08-14 嗰份嘅數，斷咗就冇人交。

---

## 【5. 一句總結】

本週 code 質素冇問題（CI 30 次全綠、前端測試全過、`src/` 零 lint error），**問題係流程斷咗**：兩個週會冇開、清理個掣冇人撳、PROGRESS.md 落後 26 個 commit、GoCardless 連續兩週排第一冇郁。呢啲全部唔係技術問題，係「冇人做」問題。
